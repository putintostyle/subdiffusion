function q = q_weight(ts, te, dt, alpha)
    q = [];
    idx = te:-dt:ts;
    idx = ceil(idx/dt);
    idx_s = idx(1);
    idx_e = idx(end);
    for i = idx(1):-1:idx(end)
        q = [q, (te-i+1)^(1-alpha)-(te-i)^(1-alpha)];
    end
    q = q./(gamma(2-alpha)*(dt^alpha));
end



